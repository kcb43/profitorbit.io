╔══════════════════════════════════════════════════════════════╗
║                 🚨 IMMEDIATE ACTION REQUIRED                  ║
╚══════════════════════════════════════════════════════════════╝

ISSUE: Redis connection failing with ECONNRESET errors
STATUS: Workers deployed but can't connect to Redis
FIX TIME: 5 minutes

┌──────────────────────────────────────────────────────────────┐
│ STEP 1: GET CORRECT REDIS URL                                │
└──────────────────────────────────────────────────────────────┘

1. Visit: https://console.upstash.com/redis
2. Click your database (or create one: "orben-redis", us-east-1)
3. Copy the "REDIS_URL" (NOT "REST URL")
4. Must look like: rediss://default:AbCd1234...@host.upstash.io:6379
   ⚠️ Note the DOUBLE 's' in rediss://

┌──────────────────────────────────────────────────────────────┐
│ STEP 2: UPDATE FLY.IO SECRETS (Run These Commands)           │
└──────────────────────────────────────────────────────────────┘

fly secrets set REDIS_URL="rediss://default:PASSWORD@HOST:6379" -a orben-deal-worker

fly secrets set REDIS_URL="rediss://default:PASSWORD@HOST:6379" -a orben-search-worker

fly secrets set REDIS_URL="rediss://default:PASSWORD@HOST:6379" -a orben-api

(Apps will restart automatically in 30 seconds)

┌──────────────────────────────────────────────────────────────┐
│ STEP 3: VERIFY FIX                                            │
└──────────────────────────────────────────────────────────────┘

fly logs -a orben-deal-worker | Select-Object -Last 30

GOOD SIGNS:
  ✅ "Redis connected successfully"
  ✅ "Supabase connection verified"
  ✅ "Polling X sources..."

BAD SIGNS:
  ❌ "ECONNRESET"
  ❌ "MaxRetriesPerRequestError"

┌──────────────────────────────────────────────────────────────┐
│ STEP 4: RUN TESTS                                             │
└──────────────────────────────────────────────────────────────┘

.\test-deployment.ps1

OR manually:

curl https://orben-api.fly.dev/v1/health
curl https://orben-api.fly.dev/v1/deals/feed?limit=5

┌──────────────────────────────────────────────────────────────┐
│ EXPECTED TIMELINE                                             │
└──────────────────────────────────────────────────────────────┘

NOW        → Fix Redis (5 mins)
+5 MINS    → Workers running successfully
+30 MINS   → First deals ingested
+2 HOURS   → 100+ deals in database
+24 HOURS  → 500-2000 deals, all sources active

┌──────────────────────────────────────────────────────────────┐
│ HELP DOCS                                                     │
└──────────────────────────────────────────────────────────────┘

FIX_REDIS.md        → Detailed Redis troubleshooting
STEP_5_TESTING.md   → Complete testing guide
STEP_5_SUMMARY.md   → What I did + what you need to do
test-deployment.ps1 → Automated test script

╔══════════════════════════════════════════════════════════════╗
║  Once Redis is fixed, you're DONE! System runs automatically ║
╚══════════════════════════════════════════════════════════════╝
